# ============================================
# LLM Portfolio Frontend - Environment Config
# ============================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# ============================================
# REQUIRED: NextAuth.js Configuration
# ============================================
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here

# For production, set to your domain (without trailing slash)
NEXTAUTH_URL=http://localhost:3000

# Trust the host header (required for Vercel / reverse-proxy deployments)
AUTH_TRUST_HOST=true

# ============================================
# REQUIRED: Credentials Login
# ============================================
# Comma-separated username:password pairs
# Example: admin:mypassword,friend1:theirpassword
AUTH_USERS=admin:changeme

# ============================================
# OPTIONAL: Google OAuth Configuration
# ============================================
# Get these from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
# If not set, Google sign-in button still shows but won't work

GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Comma-separated list of email addresses allowed to sign in via Google
ALLOWED_EMAILS=your.email@gmail.com

# ============================================
# REQUIRED: Backend API Configuration
# ============================================
# Local development (FastAPI on localhost):
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (your EC2 backend via Nginx):
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# API secret key for authenticating with FastAPI backend
# Must match the API_SECRET_KEY in your FastAPI environment
# Generate with: openssl rand -hex 32
API_SECRET_KEY=your_api_secret_key_here

# ============================================
# OPTIONAL: Direct Supabase Connection
# ============================================
# Only needed if frontend queries Supabase directly (not recommended)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ============================================
# OPTIONAL: Feature Flags
# ============================================
NEXT_PUBLIC_ENABLE_CHAT=true
NEXT_PUBLIC_ENABLE_WATCHLIST=true
NEXT_PUBLIC_ENABLE_PAPER_TRADING=false

# ============================================
# OPTIONAL: Analytics
# ============================================
# NEXT_PUBLIC_POSTHOG_KEY=
